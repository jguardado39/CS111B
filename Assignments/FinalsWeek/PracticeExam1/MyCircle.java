package Assignments.FinalsWeek.PracticeExam1;

import Assignments.Week5.Circle;

public class MyCircle {
    /*
    * Imagine that you have access to a class named MyCircle that has  void setRadius(double r) and double getRadius()  *
    * methods. Write a static method that accepts a MyCircle array. The method should accomplish two goals: it should   *
    * return the average of the radius’s for all MyCircles that have a positive radius, and for any MyCircles that have *
    * a negative radius it should set the radius to 0.                                                                  *
    */

//     private static double radius;

//     public void setRadius(double r) {
//         radius = r;
//     }

//     public double getRadius() {
//         return radius;
//     }

//     public static double averageRadius(MyCircle[] c) {
//         /*
//          * The method should accomplish two goals: it should * return the average of the
//          * radius’s for all MyCircles * that have a positive radius, and for any
//          * MyCircles that * have a negative radius it should set the radius to 0. *
//          */
//         double total = 0, average = 0;
//         int count = 0;
//         for (int i = 0; i < c.length; i++) {
//             if (c[i].getRadius() > 0) {
//                  total += c[i].getRadius();
//                  count++;
//              } else {
//                  c[i].setRadius(0);
//              }
//          }

//          return average;
//     }

//     public static void main(String[] args) {
//         // MyCircle c[3];
//         // c[0] = new MyCircle().setRadius(5);
//         // c[1] = new MyCircle().setRadius(5);
//         // c[2] = new MyCircle().setRadius(5);

//         System.out.println(averageRadius(c));
//     }
// }
}